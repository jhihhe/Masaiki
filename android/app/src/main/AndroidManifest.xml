<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- No INTERNET, no location, no camera. All processing is local. -->
    <!-- READ_MEDIA_IMAGES is only required if the app browses images itself;
         when using the Photo Picker (ActivityResultContracts.PickVisualMedia)
         no permission is required on any API level. -->

    <!-- Scoped storage-friendly: we write via MediaStore. No WRITE_EXTERNAL_STORAGE. -->

    <application
        android:name=".MasaikiApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Masaiki"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Masaiki">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
